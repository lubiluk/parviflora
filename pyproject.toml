[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "parviflora"
version = "0.1.0"
description = "RL"
authors = [{ name = "Paul Gajewski" }]
requires-python = ">=3.11"
dependencies = [
    "gymnasium",
    "numpy",
    "torch",
    "tqdm",
    "tensorboard",
    "wandb",
    "gymnasium-robotics>=1.4.2",
    "e3nn>=0.6.0",
]

[tool.hatch.build.targets.wheel]
packages = ["parviflora"]

[tool.uv.sources]
torch = [
  { index = "pytorch-cu124", marker = "sys_platform != 'darwin'" },
]

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true
